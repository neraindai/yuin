const SmallBadge = ({title}) => {
    return(
        <>
        <span className="text-xs lg:text-sm px-3 py-1 px-5 rounded-full bg-[#E0F4E8] text-[#000]">{title}</span>
        </>
    );
};
export default SmallBadge;